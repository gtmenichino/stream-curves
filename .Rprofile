## StreamCurves project profile

options(
  scipen = 999,
  digits = 6,
  warn = 1,
  stringsAsFactors = FALSE,
  readr.show_col_types = FALSE,
  repos = c(CRAN = "https://cloud.r-project.org")
)

set.seed(42)

if (interactive()) {
  cli::cli_alert_info(
    "StreamCurves project loaded. Install required packages from README.md, then run {.code shiny::runApp('app')} or {.code source('run_all.R')}."
  )
}
